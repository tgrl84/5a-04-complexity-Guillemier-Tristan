class AlgoTester{
    constructor(){
        this.testSuit = [];
        this.algorithmes = [];
    }

    addTestSuit(name,tests= []){
        const suit = {nom : nom,tests :tests};
        this.testSuit.push(suit);
    }

    addAlgo(algoName, algo) {
        this.algorithmes[nomAlgo] = implementation;
    }
}